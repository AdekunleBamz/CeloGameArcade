# SDK Zero Decimal Token Check

- Configure a zero-decimal token in an SDK integration test scenario.
- Confirm entry fees are passed explicitly in base units.
- Verify fractional display copy is not shown for non-fractional tokens.
