# SDK README Example Check

Run through SDK README examples and confirm they match current helper names.
Record the example, package version, and result.
Update docs before release if examples drift.
