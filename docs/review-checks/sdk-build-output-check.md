# SDK Build Output Check

Build the SDK package and confirm generated dist files match expected exports.
Record the package version, build command, and output directory.
Do not commit generated secrets or local paths.
